version: "3.9"

services:
  skc-suggestion-enging:
    image: golang:1.18.3-bullseye
    command: go run .
    working_dir: /app
    ports:
      - "9001:9000"
    volumes:
      - "./:/app"
    restart: always
    deploy:
      resources:
        limits:
          memory: 80m
    networks:
      - skc-network
    environment:
      - SKC_SUGGESTION_ENGINE_DOT_ENV_FILE=.env_docker

networks:
  skc-network:
    name: skc-network
    driver: bridge